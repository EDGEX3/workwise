import 'package:flutter/material.dart';

    class Sells extends StatelessWidget {
      const Sells({super.key});
    
      @override
      Widget build(BuildContext context) {
        return const Text('Sells');
      }
    }