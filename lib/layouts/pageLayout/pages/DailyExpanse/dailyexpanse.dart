import 'package:flutter/material.dart';

    class DailyExpanse extends StatelessWidget {
      const DailyExpanse({super.key});
    
      @override
      Widget build(BuildContext context) {
        return const Text('DailyExpanse');
      }
    }