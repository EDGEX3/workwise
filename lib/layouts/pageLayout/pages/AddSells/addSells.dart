import 'package:flutter/cupertino.dart';

class AddSells extends StatelessWidget {
  const AddSells({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("AddSells");
  }
}
